# Disabled action history configuration
# For maximum performance when history is not needed

# Core settings
enabled: false
max_history_length: 100  # Minimal if enabled

# Storage options
store_selection_data: false
store_intermediate_grids: false
compress_repeated_actions: true