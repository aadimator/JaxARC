# @package _global_
# Debug mode full - complete state dumps and analysis

defaults:
  - /visualization: debug_full
  - /storage: research
  - /logging: local_only

# Legacy settings (for backward compatibility)
log_rl_steps: true
rl_steps_output_dir: "outputs/rl_steps_full"
clear_output_dir: false  # Don't clear for full mode

# Enhanced visualization settings
enhanced_visualization:
  enabled: true
  level: "full"