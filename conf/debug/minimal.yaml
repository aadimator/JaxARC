# @package _global_
# Debug mode minimal - episode summaries only

defaults:
  - /visualization: debug_minimal
  - /storage: development
  - /logging: local_only

# Legacy settings (for backward compatibility)
log_rl_steps: true
rl_steps_output_dir: "outputs/rl_steps_minimal"
clear_output_dir: true

# Enhanced visualization settings
enhanced_visualization:
  enabled: true
  level: "minimal"